# Ejercicio de Warm-up 7

"""

Given an int n, return True if it is within 10 of 100 or 200. Note: abs(num) computes the absolute value of a number.

Examples:
near_hundred(93) → True
near_hundred(90) → True
near_hundred(89) → False
"""


def near_hundred(n):
    n_1 = abs(n - 100)
    n_2 = abs(n - 200)
    if n_1 <= 10 or n_2 <= 10:
        return True
    else:
        return False
